#pragma once

enum class LedState { OFF, FOCUSED, ON };
enum class PatternState { NORMAL, MIRROR_H, MIRROR_V, MIRROR_HV };
enum class PatternType { DEFAULT, ANIMATION };
enum class DisplayAmount { x1, x4 };